name: 05 - 2 - Event Filters and Activity Types

on:
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Print another message
        run: echo "unning whenever a PR is closed"
